Class {
	#name : #GHMTableBlockTest,
	#superclass : #GHMBlockTest,
	#category : #'GithubMarkdown-Tests'
}

{ #category : #running }
GHMTableBlockTest >> setUp [
	"Hooks that subclasses may override to define the fixture of test."

]
